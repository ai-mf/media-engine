# media-engine/services/batch/Cargo.toml
[package]
name = "media-engine-batch"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow ={workspace=true}
glob = {workspace=true}
tempfile = {workspace=true}